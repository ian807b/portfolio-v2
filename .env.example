# ─── Server Configuration ───
NODE_ENV=development
PORT=4000

# ─── Database ───
# Local development uses SQLite (no external services needed):
DATABASE_URL=file:./dev.db
#
# For production with PostgreSQL, use:
# DATABASE_URL=postgresql://portfolio:portfolio_dev_password@localhost:5432/portfolio
# And change prisma/schema.prisma provider to "postgresql"

# ─── CORS ───
CORS_ORIGIN=http://localhost:3000

# ─── Security ───
# IMPORTANT: Change this to a random secret in production
IP_HASH_SALT=change-this-to-a-random-secret

# ─── Rate Limiting ───
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX=100  # General API limit
MCP_RATE_LIMIT_MAX=10  # MCP chat limit per 15min (adjust based on your cost tolerance)

# ─── Anthropic API (Optional) ───
# For AI-powered MCP chat responses. Get your API key from:
# https://console.anthropic.com/settings/keys
# If not set, MCP will use keyword matching fallback.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ─── Frontend (Vite) ───
# Leave empty for local dev (Vite proxy handles /api routes).
# Set to the backend URL for production builds.
# VITE_API_URL=https://api.ianh0123.com
